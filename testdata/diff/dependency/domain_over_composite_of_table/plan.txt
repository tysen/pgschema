Plan: 3 to add.

Summary by type:
  types: 1 to add
  tables: 1 to add

Types:
  + mid

Tables:
  + foo

DDL to be executed:
--------------------------------------------------

CREATE TABLE IF NOT EXISTS foo (
    id uuid
);

CREATE TYPE mid AS (f foo);

CREATE DOMAIN d AS public.mid;
